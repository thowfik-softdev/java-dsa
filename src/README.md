# src/ — ☕ Your runnable Java

**Why this folder exists:** the actual code you write and run, mirroring [../02-problems/](../02-problems/).

Run loop (remember: **compile the file, run the name**):
```bash
cd src
javac Day1.java && java Day1
```
One file per problem/day. `.class` build files are git-ignored.
