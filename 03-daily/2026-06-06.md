---
type: daily
date: 2026-06-06
day: 3
---

# Day 3 — 2026-06-06

**Roadmap target:** 0.4 Loops (for / while / do-while / enhanced-for / break-continue)

## 0. Setup (this device)
- Installed **Temurin JDK 21 LTS** via winget; verified `javac`/`java` compile+run end-to-end.
- Fixed off-by-one daily-log dates (Day 1 = 06-04, Day 2 = 06-05) — committed.

## 1. Recall (quick)
- Quizzed [[Conditional Statements]] (due today): `.equals()` for Strings, no-break switch (`x=2` → two/three/other), chained if/else if vs separate ifs. **3/3 correct by reasoning** → card pushed out ~3 days.

## 2. Learn
- [[Loops]] — four shapes mapped to JS (only diff: typed counter). `do-while` runs body once (check at bottom). Enhanced-for `:` = JS `of`.
- Big-O preview: one loop = O(n), nested = O(n²).

## 3. Practice + 4. Solve (by hand)
- Wrote `T04_Loops.java` BY HAND — all four loop shapes + break/continue, run clean.
- **Debugged a real bug from output:** `break` at 15 wasn't firing because `15 % 3 == 0` made `continue` fire first (continue skips lines below it). Fixed by moving the `break` check above the `continue`. Understood the *why*.
- Also caught/fixed: `do-while` condition direction, a missing semicolon, and the "one `public class` per file" rule (had 5 separate class blocks → merged into one runnable `main`).

## 5. Notes generated
- [[Loops]] (01-concepts/loops.md) — includes his actual code + the continue/break ordering lesson.

## 👤 Reflection (you)
- What clicked: `continue` skips everything below it (order matters); one class/one main per file.
- Strong signal: found and fixed the break bug by *reading the output* — real debugging instinct.
- Energy (1–5): _(fill in)_
