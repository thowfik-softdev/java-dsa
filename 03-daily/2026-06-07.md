---
type: daily
date: 2026-06-07
day: 4
---

# Day 4 — 2026-06-07

**Roadmap target:** 0.5 Methods (declaring, parameters, return types, static, void)

## 1. Recall (quick)
- [[Loops]] card (due today): named all 4 loop shapes; `continue` skips current iteration + lines below (explained the break-at-15 bug correctly); enhanced-for `:` = JS for-of; linear scan = O(n)/O(1).
- One gap: didn't state `do-while` runs **at least once** (checked at bottom) → corrected. Card pushed to ~3 days.

## 2. Learn
- [[Methods]] — named reusable block = JS function + types. Anatomy: `static returnType name(typed params)`. `void` = returns nothing. `static` = no object needed (DSA methods always static).
- Realized he'd already written 3 methods yesterday (`findMax`, `runTest`, `main`).

## 3. Practice + 4. Solve (by hand)
- Wrote `T05_Methods.java` BY HAND: `add`, `square`, `greet` (void), `isEven`. Ran clean.
- **Bug caught:** `greet` printed literal `"<name>"` instead of concatenating the parameter → lesson: a parameter is a VARIABLE, not literal text.
- **Smell caught + reverted:** changed `isEven` to `return cond ? true : false;` then reverted to `return n % 2 == 0;` after the "boolean ternary is redundant" lesson.

## 5. Notes generated
- [[Methods]] (01-concepts/methods.md) — with his actual annotated code.

## Workflow decisions today
- Adopted **Concept Scaffold** (Mentor creates the file with simple guiding comments, learner fills via chat guidance) — documented in RULES + CONTEXT.

## 👤 Reflection (you)
- What clicked: `static`/`void`, parameter = variable (not literal), the boolean-ternary smell.
- Next: one methods practice problem (sumArray) via Test-First Drill.
- Energy (1–5): _(fill in)_
